# Deprecated

**Deprecated — use [`veridion-nexus-mcp`](https://www.npmjs.com/package/veridion-nexus-mcp) instead.** The Sovereign Shield MCP tools ship as the default entry point of that package (`npx -y veridion-nexus-mcp` with `VERIDION_NEXUS_API_KEY`).

This standalone `veridion-shield-mcp` folder is kept for reference only; it may be removed in a future release.
